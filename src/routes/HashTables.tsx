import * as React from 'react';

interface IHashTablesProps {
}

const HashTables: React.FunctionComponent<IHashTablesProps> = (props) => {
  return (
    <>
    <h1>In Development</h1>
      {/* <input type="text" />
      <button>Append</button>
      <button>Prepend</button>
      <button>PopFirst</button>
      <button>PopLast</button>
      <button>Clear</button> */}
    </>
  );
};

export default HashTables;
